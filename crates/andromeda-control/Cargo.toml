[package]
name = "andromeda-control"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Control plane: VPC/endpoint model and overlay forwarding (VNI + inner-IP -> underlay node) lookup"

[dependencies]
andromeda-core = { workspace = true }
